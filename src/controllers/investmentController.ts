import { Request, Response } from "express";
import InvestmentData from "../models/InvestmentData";
// import { writeDashboardData } from "../utils/dashboarddata-generator";

export const getInvestmentData = async (req: Request, res: Response) => {
//   try {
    // await writeInvestmentData(res.locals.user._id);
    // const data = await InvestmentData.findOne({
//       user: res.locals.user._id,
//     }).select({ _id: false, user: false, __v: false });
//     res.json(data);
//   } catch (err) {
//     console.log(err);
//     res.json({ msg: "server error" });
//   }
};
